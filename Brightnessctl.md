# Brightnessctl - CLI

Read and control device brightness

→ List devices with available brightness controls.
`brightnessctl --list`

→ Set the brightness to 50% of the device selected from the list, for example `intel_backlight`
`sudo brightnessctl --device=intel_backlight set 50%`
